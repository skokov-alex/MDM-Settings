Если ОбъектОбработки.ДублирующаяПозиция тогда  
	параметры.ЗаявкаОбъект.РезультатВыполнения = "Найден дубль"; 
	параметры.ЗаявкаОбъект.ОтклонитьЗаявку = истина; 
	параметры.ЗаявкаОбъект.ИзменяемыеОбъекты.Очистить(); 
конецесли;    
 
если ЗначениеЗаполнено(СокрЛп(ОбъектОбработки.РезультатВыполненияСтрока)) тогда  
	параметры.ЗаявкаОбъект.НадоУточнить = Истина;     	 
	параметры.ЗаявкаОбъект.ОписаниеЗадания = СокрЛп(ОбъектОбработки.РезультатВыполненияСтрока); 
	//2=1; 
конецесли;    

